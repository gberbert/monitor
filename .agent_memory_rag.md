# AGENT MEMORY RAG

## Histórico de Alterações

### [2025-12-26 13:25:00] - Inicialização do RAG
- **O que foi feito**: Criação do arquivo de memória persistente.
- **Motivo**: Garantir continuidade e registro de decisões arquiteturais.
- **Snapshot**: N/A (Arquivo novo).

### [2025-12-26 13:35:00] - Correção dos Campos do Modal de Câmera
- **O que foi feito**: 
  - Atualização do `dashboard.html` para incluir inputs de `RTSP URL` e `Timeout` no modal de edição/criação.
  - Atualização do `desktop_app/database.py` para adicionar coluna `timeout` e persistir esse dado.
  - Atualização do `vms_proxy.py` para processar `rtsp_url` e `timeout` na rota `/api/save_camera`.
- **Motivo**: O usuário precisava editar manualmente a URL RTSP e o Timeout (especialmente para câmeras instáveis), mas os campos não apareciam na interface nem eram salvos.

### [2025-12-26 13:32:00] - Correção de Status Offline para Câmeras com Acento
- **O que foi feito**: Atualização do `vms_proxy.py` para incluir normalização de acentos (`unicodedata`) ao buscar streams ativos.
- **Motivo**: Câmeras como "Portão" eram salvas corretamente como "portao" no `go2rtc.yaml`, mas o backend buscava por "porto" (removendo o 'ã' via regex simples), causando falso status "OFFLINE" no dashboard mesmo com a câmera funcionando.

### [2025-12-26 13:38:00] - Ajuste de Responsividade do Modal
- **O que foi feito**: Adicionado `max-height: 90vh` e `overflow-y: auto` ao CSS da classe `.modal` no `dashboard.html`.
- **Motivo**: O modal de configuração estava cortando em telas menores ou com zoom, impedindo acesso aos botões de ação. Agora ele escala verticalmente e permite rolagem.

### [2025-12-26 13:50:00] - Criação do Ambiente de Testes Isolado (HD)
- **O que foi feito**:
  - Criada pasta `test_lab_hd` com cópia isolada de `www` e `desktop_app`.
  - Criado `go2rtc_test.yaml` rodando nas portas API:1985, RTSP:8556, WebRTC:8557.
  - Criado `vms_proxy_test.py` rodando na porta 5001.
  - Criado `sync_test.py` para gerenciar configuração sem afetar produção.
  - Criado launcher `start_test_env.bat`.
- **Motivo**: Preparação para desenvolvimento da funcionalidade de troca de stream HD/SD sem impactar o sistema principal.

### [2025-12-26 14:26:00] - Correção Crítica de Caminho FFmpeg (ANTI-REGRESSÃO)
- **O que foi feito**:
  - Substituição de todas as referências hardcoded a `C:\antigravity_www\ffmpeg.exe` pela referência dinâmica: `os.path.join(BASE_DIR, "go2rtc_bin", "ffmpeg.exe")`.
  - Arquivos corrigidos: `vms_proxy.py`, `sync_cameras_to_web.py`, `App_Antigravity.bat`, `fix_icsee.py` e `go2rtc_bin/go2rtc.yaml`.
- **Motivo**: A aplicação parava de exibir vídeos porque o caminho do FFmpeg estava sendo revertido para um diretório inexistente (`antigravity_www`) pelo script de sincronização.
- **REGRA CRÍTICA**: NUNCA mais usar o caminho `C:\antigravity_www`. Tudo deve ser relativo ao diretório do projeto ou usar a pasta `go2rtc_bin` interna. O diretório antigo foi renomeado para `antigravity_www_TESTE` e não deve ser referenciado.
